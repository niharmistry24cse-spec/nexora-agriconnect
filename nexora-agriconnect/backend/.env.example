# Copy this file to .env and fill in your keys.
# The backend works fine with no keys — it will just use mock data as a fallback.

# Get a free key at https://data.gov.in (My Account -> Generate API Key)
# Used for real mandi/market prices in /api/market-prices
AGMARKNET_API_KEY=
PORT=5000
MONGO_URI=mongodb+srv://niharmistry24cse_db_user
